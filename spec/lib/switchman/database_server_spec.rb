describe SwitchmanInstJobs::Switchman::DatabaseServer do
  describe '#create_new_shard_with_delayed_jobs' do
    it 'sets send_later_enqueue_args aliased method chain'
  end

  describe '#delayed_jobs_shard' do
    it 'returns given shard if configured as self'

    it 'returns configured shard if specified'

    it 'returns default shard if specified'

    it 'returns delayed jobs shard configured on default shard'
  end
end
